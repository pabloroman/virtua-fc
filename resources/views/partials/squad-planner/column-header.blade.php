{{-- Desktop column header for planner player tables. --}}
<div class="hidden md:grid grid-cols-[40px_1fr_140px_48px_72px_180px_48px] gap-3 items-center px-4 py-2 bg-surface-700/30 border-b border-border-default text-[10px] text-text-muted uppercase tracking-widest font-semibold">
    <span></span>
    <span>{{ __('app.name') }}</span>
    <span class="text-right">{{ __('planner.col_action') }}</span>
    <span class="text-center">{{ __('app.age') }}</span>
    <span class="text-center">{{ __('app.contract') }}</span>
    <span>{{ __('squad.potential') }}</span>
    <span></span>
</div>
