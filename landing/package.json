{
    "name": "virtua-fc-landing",
    "private": true,
    "scripts": {
        "dev": "npx wrangler pages dev public --d1 DB=virtua-fc-waitlist",
        "deploy": "npx wrangler pages deploy public"
    },
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
